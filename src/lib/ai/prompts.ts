/**
 * Prompt templates for AI features
 */

import { type DigestTasksInput } from "./types";

/**
 * Prompt for generating task details from title
 */
export function buildTaskGenerationPrompt(title: string): string {
  return `Ты помощник для менеджера задач. На основе заголовка задачи сгенерируй детали.

Заголовок задачи: "${title}"

Сгенерируй:
1. description - развёрнутое описание задачи (2-4 предложения на русском)
2. priority - приоритет (LOW для рутинных задач, MEDIUM для обычных, HIGH для важных, URGENT для срочных/критичных)
3. deadlineDays - через сколько дней дедлайн (число от 1 до 30)

Ответь ТОЛЬКО валидным JSON без markdown:
{"description": "...", "priority": "MEDIUM", "deadlineDays": 7}`;
}

/**
 * Prompt for generating daily digest
 */
export function buildDigestPrompt(data: DigestTasksInput): string {
  const today = new Date().toLocaleDateString("ru-RU", {
    weekday: "long",
    day: "numeric",
    month: "long",
  });

  return `Ты помощник для планирования рабочего дня. Проанализируй задачи и составь краткий план.

Сегодня: ${today}

Статистика задач:
- Всего активных: ${data.total}
- К выполнению (TODO): ${data.todo}
- В работе: ${data.inProgress}
- На проверке: ${data.review}
- Выполнено: ${data.done}
- Просроченных: ${data.overdue}

Список активных задач:
${data.tasksList || "Нет задач"}

Сгенерируй план дня на русском языке в формате:

## 🎯 План на сегодня
[2-3 приоритетные задачи с которых стоит начать, с кратким обоснованием]

## ⚠️ Внимание
[Задачи с близкими дедлайнами или просроченные, если есть]

## 💡 Рекомендация
[1 короткий совет по организации работы]

Будь кратким и конкретным. Не более 200 слов.`;
}
